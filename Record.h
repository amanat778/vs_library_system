#ifndef RECORD_H
#define RECORD_H


class Record
{
    private:
        string filename;
        string *fields;
        int length;
    public:
        Record();
        ~Record();

    protected:


};

#endif // RECORD_H
